# ObsidianWebClipper

## Require
```yaml
node: 16
yarn: *
```

## Installation

```shell
yarn
```

## Build

Run `yarn build` to build the project. The built artifacts will be stored in the `dist/` directory.
